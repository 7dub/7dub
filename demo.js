function hello() {
	var obj ={
		"name":"1111",	
		"desc":"this is a string from 7dub"
	};
	return JSON.stringify(obj);
}

function analyse() {
	var obj ={
		"page":"native home",
		"uid":"v1456666",
		"desc":"this is a string from 7dub"
	};
	return JSON.stringify(obj);
}
